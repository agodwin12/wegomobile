<?xml version="1.0" encoding="utf-8"?>
<!--
  WEGO network security policy.
  Production traffic is HTTPS only (cleartext denied by default).
  Cleartext is permitted ONLY for local development hosts so that
  `flutter run` against a localhost backend works:
    - 10.0.2.2  → Android emulator's alias for the host machine
    - localhost / 127.0.0.1 → desktop / USB-forwarded testing
  Remove the raw-IP entry once the backend is behind HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Legacy raw-IP backend over HTTP — delete after HTTPS cutover. -->
        <domain includeSubdomains="false">57.128.178.119</domain>
    </domain-config>
</network-security-config>
